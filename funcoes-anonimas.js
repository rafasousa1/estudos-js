// FUNÇÕES ANÔNIMAS

// São funções que não tem nome

const multiplicar = function(a, b) { // PASSANDO A FUNÇÃO EM UMA VARIÁVEL
    return a * b 
}

console.log(multiplicar(5,5)) // E só são acessadas depois, se colocar a execução antes de escrever a função ele não roda